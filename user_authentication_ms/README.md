(draft) Set up instructions to run containerized:
* Create a `.env.production` file and fill it according to `.env.sample`
* Make sure to expose the same port in `Dockerfile.backend` as the `APP_PORT` env variable
* `docker compose up`
