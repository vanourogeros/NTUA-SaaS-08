export default function Table({ theadData, tbodyData }) {
    return (
        <table>
            <thead>
                <tr>

                </tr>
            </thead>
            <tbody>

            </tbody>
        </table>
    );
}