import CreateChart from "components/CreateChart";

export default function CreateBasicColumn() {
    return (
        <CreateChart
            chartType="organization"
            chartName="Organization"
            chartUrlType="organization"
        ></CreateChart>
    );
}
